{
  "name": "nlpverifier",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "full": "cd frontend/ && npm run build && cd .. && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "fs-extra": "^5.0.0",
    "multer": "^1.3.0"
  }
}
